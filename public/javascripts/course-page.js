/* -------------------- load url to get parameters --------------------- */        
/*$(document).ready(function() {
    if(!url('query')){
        var urls = url();
        urls += '?q=course';
        window.location = urls;        
    }
});